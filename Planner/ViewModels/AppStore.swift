// AppStore was removed in Phase 1.
//
// Tasks, notes, and goals are now stored in SwiftData and queried directly
// in views via `@Query`. Calendar logic moved to `TaskCalendarService`.
//
// This file is intentionally empty. You can delete it from the Xcode
// project navigator at your convenience — kept as an empty stub only so
// the existing project file reference does not produce a "missing file"
// error on first open.
